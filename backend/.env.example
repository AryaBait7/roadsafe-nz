# Copy to .env if you need to override. All optional.
# PORT=4000
# Comma-separated origins allowed to call the API.
# CORS_ORIGIN=http://localhost:3100
# Where the pipeline's aggregates live. Defaults to ../frontend/src/data/fixtures
# until the Stage 18 database replaces them.
# FIXTURES_DIR=../frontend/src/data/fixtures
